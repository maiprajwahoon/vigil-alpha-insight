# Debug Session: route-import-fetch

- Status: OPEN
- Symptom: Browser reports `TypeError: Failed to fetch dynamically imported module: http://127.0.0.1:4174/src/routes/index.tsx?tsr-split=component`
- Scope: Dev preview on local Vite server
- Constraints: No business logic changes before runtime evidence

## Hypotheses

1. Vite returns HTTP 500 for the split route module request.
2. The detached preview process serves HTML but fails TSX split-module transforms.
3. A dependency imported by `src/routes/index.tsx` throws during transform.
4. The route-splitting URL/path generated by TanStack Router is incorrect in this environment.
5. Vite dependency optimization is incomplete, causing only dynamic imports to fail.

## Evidence Log

- `vite-4174-stderr.log` shows `ERR_MODULE_NOT_FOUND` for `node_modules/motion-dom/dist/es/animation/utils/WithPromise.mjs`.
- `vite-4174-stderr.log` shows unresolved imports for `./animation/waapi/supports/waapi.mjs` and `./render/VisualElement.mjs` from `motion-dom/dist/es/index.mjs`.
- `node_modules/motion-dom/package.json` reports version `12.42.0`.
- Files referenced by `motion-dom` imports are missing on disk:
  - `dist/es/animation/utils/WithPromise.mjs`
  - `dist/es/animation/waapi/supports/waapi.mjs`
  - `dist/es/render/VisualElement.mjs`
- The browser error `Failed to fetch dynamically imported module ... index.tsx?tsr-split=component` happens after Vite dependency optimization fails.

## Hypothesis Status

1. Vite returns HTTP 500 for the split route module request. -> Supported by Vite stderr and browser error.
2. The detached preview process serves HTML but fails TSX split-module transforms. -> Partially supported, but secondary to broken dependency contents.
3. A dependency imported by `src/routes/index.tsx` throws during transform. -> Confirmed (`motion` / `motion-dom`).
4. The route-splitting URL/path generated by TanStack Router is incorrect in this environment. -> Rejected by dependency evidence.
5. Vite dependency optimization is incomplete, causing only dynamic imports to fail. -> Confirmed as a symptom; root cause is malformed installed package contents.

## Actions

- Collected Vite runtime evidence from `vite-4174-stderr.log`.
- Verified missing files directly under `node_modules/motion-dom/dist/es`.
- Next: repair `motion` dependency installation and clear Vite optimized deps cache, then re-verify module loading.
